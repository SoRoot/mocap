<% if @motion_record.errors.any? %>
	<div class="mocap-textbox error-field">
		The form contains <%= pluralize(@actor.errors.count, "error") %>
		<ul>
			<% @motion_record.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
